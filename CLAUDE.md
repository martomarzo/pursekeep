@AGENTS.md

# Project instructions (Money Maker)

- **Never add a Claude/AI co-author trailer or attribution to git commits in this repo.** Plain commit messages only.
- `plan.md` is the source of truth for design; keep it updated when decisions change.
- Money is integer minor units ("cents") in app code (`src/lib/domain/money.ts`), `numeric(14,2)` strings at the DB/API boundary. PYG has zero decimals.
- Transfer transactions are two linked rows (`transfer_peer_id`); transfer legs have SIGNED amounts (negative = outflow), expense/income are always positive.
- `server_seq` on sync-tracked tables is stamped by a DB trigger (migration 0001) — never set it in app code.

## Session status (update this section at every good stopping point)

_Last updated: 2026-09-22_

- **Phase 0 complete** (scaffold, schema+migrations, auth/household/invite flow, Docker+Tailscale compose, CI+deploy workflows, money tests). Verified: lint, typecheck, tests, build.
- **Phase 1 complete**: FX module (`src/lib/fx/`, boot+daily refresh via `src/instrumentation.ts`), visibility-aware queries (`src/lib/queries.ts`), account+transaction actions, accounts UI, transactions UI (list with filters + per-currency/base summaries, add/edit forms incl. cross-currency transfers). Verified: lint, typecheck, tests, build. NOT yet tested against a live database — first end-to-end run happens on deploy (or local Postgres).
- **Server setup not done yet** (see README Deployment): self-hosted runner on the docker host, `/opt/money-maker/.env`, one-time `TS_AUTHKEY`.
- **Phase 1.5 bank imports — EXTRACTION DONE on the Windows machine.** `scripts/extract/` (Python stdlib + `pdftotext`) parses `BANCOS/` (gitignored) into normalized JSON at `data/imports/extracted/` (gitignored, synced via the Drive share at `/mnt/drive/Stuff/Code/07_money-maker` — copied to this machine already): 58 statements, 2,273 rows, 2023-12→2026-08, ARS/EUR/PYG/USD, all validated arithmetically. JSON contract: `scripts/extract/README.md`.
- **Phase 1.5 app side COMPLETE (2026-08-08):** migration 0002 (`import_batches`, `category_rules` incl. server_seq trigger, `transactions.original_amount/original_currency`); import engine `src/lib/import/` (zod contract, kind→type mapping, dedupe, transfer-leg matching, accent-insensitive rules — all 2,273 real rows parse clean); `/import` UI + actions (preview→dedupe→categorize→commit, idempotent via `onConflictDoNothing` on the partial (account_id, source_hash) index, batch undo clears source_hash so re-import works, household-wide `matchUnlinkedTransfers` sweep); `/settings/categories` UI + `category_rules` management + seed rules at household creation; `scripts/backfill-fx.ts` (`npm run fx:backfill -- --dry-run` works; frankfurter EUR→USD, BCRA composed for ARS, fxratesapi for PYG — BCP is Cloudflare-403). Verified: lint, typecheck, tests (80), build. STILL not tested against a live database.
- Note: cross-currency exchange legs (Revolut/Wise) only auto-link within one statement file; the DB-side sweep is same-currency only (kind isn't persisted). In practice transfer legs pair across files — link remaining ones manually or extend later.
- **DEPLOYED AND LIVE (2026-08-08 evening): `https://money-maker.peacock-snapper.ts.net`** — first live-DB run; migrations 0000–0002 applied on boot. Server (`tailscale ssh root@docker`, Debian 13, tailnet `peacock-snapper.ts.net`): `/opt/money-maker/.env` (root:runner 640), runner `docker-host` at `/opt/actions-runner` as systemd service with Restart=on-failure, +4G swapfile (`/swapfile`, in fstab — the box OOM-killed the first build AND the runner itself without it). `scripts/server-setup.sh` documents/automates this. CI note: typecheck needs `next typegen` first (fresh checkouts lack generated `LayoutProps` types) and lockfiles must be written by npm 10 (`npx npm@10 install --package-lock-only`) or CI's `npm ci` rejects them — CI had NEVER passed until tonight because of this.
- Live-DB state: user registered + household created; FX backfill DONE (1,773 rows in fx_rates, 2023-12→2026-08 — ran on the server as a throwaway node container on `docker_default` with `--env-file /opt/money-maker/.env`; NOTE mount the repo READ-ONLY or copy it inside the container first — running `npm ci` in the runner workdir left root-owned node_modules that broke the next deploy's checkout). Extracted JSON copied to `/opt/money-maker/imports` (bind-mounted ro at `/app/data/imports`); `/import` verified serving all 58 statements. User does the actual imports 2026-08-09.
- **Phase 1.7 wallet capture — SUPERSEDED (2026-08-25).** Built+deployed 2026-08-19 (migration 0003, `src/lib/wallet/`, `POST /api/wallet/capture`, `/wallet`, `/settings/devices`) but real taps never arrived and the user does not want to depend on MacroDroid/Shortcuts config. Kept as an unlinked *experimental* path (in-page note); no further parser work planned. Endpoint stays for a possible future native Android companion.
- **Phase 1.8 native quick capture + visual pass — DONE (2026-08-25):** `/add` quick-add screen (`src/components/quick-add.tsx`, keypad/account chips/category grid, device prefs in localStorage `mm.quickadd.v1`) is the target of every + button; design tokens (`src/app/globals.css` @theme), primitives (`src/components/ui.tsx`), app shell with desktop nav + mobile bottom tab bar + floating `+` (`src/components/app-shell.tsx`), dashboard rebuilt as capture hub (`/` hero with Add expense/income/transfer → `/transactions/new?type=…`, balances, recent), shared `src/components/transaction-row.tsx`, PWA manifest (`src/app/manifest.ts`) + icons (`src/app/icon.svg`, `apple-icon.png`, `favicon.ico`, `public/icons/*` generated with ImageMagick `convert`), all pages restyled with the primitives.
- **Phase 1.9 personal ledger & households — DEPLOYED + VERIFIED LIVE 2026-08-25** (migration 0004 applied cleanly; user importing/adding expenses). Note: ssh to `docker` hangs non-interactively from this machine — hand server checks to the user as `!` one-liners. Design: All accounts/categories/transactions become user-owned; `transaction_shares` (≤1 household per txn) + `transaction_share_splits` (even split default, owner-editable); `visibility` flag and `accounts.owner_user_id` removed; base currency per user and per household; migration 0004 moves existing data to the single member's ledger with zero shares. Spec with schema, queries, routes, build order: `docs/superpowers/specs/2026-08-25-personal-ledger-and-households.md`. Wallet capture `shareWalletCapture` was removed (sharing is now household-scoped via `actions/shares.ts`).
- **Phase 1.6 dashboard & budgets — DONE 2026-08-25:** `src/lib/reports.ts` aggregates (+ `netWorth`), dashboard components `src/components/dashboard/*`, `/budgets` (`src/components/budgets/*`, `src/lib/actions/budgets.ts`). Chart tokens `--chart-expense/--chart-income` in globals.css are validated colorblind-safe — keep them if adding charts (load the dataviz skill first). ssh `root@docker` works when the user has logged in on this machine (tailscale ssh) — the compose project is `docker` (containers `docker-app-1`, `docker-postgres-1`), migrations table `drizzle.__drizzle_migrations`.
- **Bulk categorization (2026-08-25):** inline picker on list rows (`src/components/row-category-picker.tsx`) + `categorizeAllMatching`/`renamePayeeEverywhere` in `src/lib/actions/categorize.ts`; `?category=none` = uncategorized filter. Transfers editable via `src/lib/actions/transfers.ts` (edit leg, unlink, convert ↔ expense/income).
- Repo renamed to `github.com/martomarzo/pursekeep` on 2026-09-22 (old name redirects; local dir still `11_money-maker`).
- **PurseKeep Android companion — DONE 2026-09-22** (`android/`, app id `app.pursekeep`, releases via tags `android-v*`, contract `docs/wallet-capture-contract.md`). Rule: any change to `src/lib/wallet/types.ts` or the capture route's responses = update `android/contract/fixtures`, both contract tests, and the Android sender; add an "Android app" line to CHANGELOG with the app version. Keystore is user-owned at `~/.pursekeep/` — never commit. Local toolchain `~/.local/android-toolchain`.
- **HOSTED ON RENDER — LIVE at `https://pursekeep.com` since 2026-09-22** (web `pursekeep` srv-dap9cg3m8hqs73a7ctkg, Postgres `pursekeep-db` dpg-dap9c4jm8hqs73a7bns0-a, PG16, Frankfurt, workspace "Personal website" `tea-d6ob7on5gffc73biduf0`; `render.yaml` is the source of truth; `AUTH_URL=https://pursekeep.com`, `REGISTRATION_MODE=invite`, DB reachable from the internet by the user's choice). Tailnet DB was `pg_dump`ed and restored (2,008 transactions, 2 users, 1,957 fx rows) — the tailnet stack is a stale fallback, to be stopped with `docker compose down` (keep volumes). Self-hosted `deploy.yaml` workflow deleted; Render auto-deploys on push to main (ignores `android/**`, `docs/**`, `*.md`, `.github/**`). `www.pursekeep.com` redirects to the apex; `pursekeep.app` is owned but not attached. Render CLI at `~/.local/bin/render` (not logged in); MCP tools need the workspaceId. Hardening: invite-only registration, login/register throttling, security headers, `/api/health`.
- **NEXT (open):** companion biometric lock (WebView shipped in 0.2.0), Play Store prep (privacy policy at pursekeep.com/privacy, data-safety form), stop the tailnet stack + decommission the runner, off-platform DB backups, offline PWA, household budgets, needs-review filter, settle-up.
- Workflow (user preference): at every stopping point update plan.md + CLAUDE.md + **CHANGELOG.md (user-facing, per session)** + memory FIRST, then a single commit+push — never commit twice for code-then-docs.
